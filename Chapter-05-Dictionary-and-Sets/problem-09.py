# Can you change the values inside a list which is contained in set S?
# s = {8, 7, 12, "Harry", [1,2]}
# Answer: No. A list cannot be stored in a set because lists are mutable (unhashable). 
# Therefore, the given set is invalid and raises a TypeError.